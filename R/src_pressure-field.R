#============================
# Pressure Field
# Alwin Wang
#----------------------------

#--- Determine smaller vector to plot over ----
# hist(diff(long_bndry$s), breaks = 20)
# mean(diff(long_bndry$s))
